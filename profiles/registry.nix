# The one place a distribution names itself. Everything else about a profile
# lives in its own directory, so this file is the only edit a fork makes to
# change which profile the menu opens on.
{
  default = "juspay";
}
